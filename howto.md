# UPDATE

To ship an update: bump VERSION (+ installer.iss MyAppVersion + launcher.cs AssemblyFileVersion), rebuild, then publish a GitHub release tagged e.g. v3.0.2 with JDrakoon3-Setup.exe attached. Installed clients update themselves on next launch.
