/.vscode

frontend-build/

# Release build artifacts (regenerated by build-release.ps1)
/release/
/JDrakoon3.exe
/node.exe
crash-output.txt

# WebView2 SDK DLLs — fetched from NuGet by build-release.ps1 (Ensure-WebView2)
/webview2/
_csc_check.rsp

# GitHub token for release.ps1 — NEVER commit
.github_token